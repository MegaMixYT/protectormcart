const Discord = require("discord.js");

module.exports.run = async (bot, message, args) => {
    message.reply("Привет я бот Protector, меня сделал MCArTyR специально для сервера World Of Music!");
}

module.exports.help = {
    name: "developer"
}
